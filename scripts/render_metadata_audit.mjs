const esc = x => String(x ?? '').replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const labels = {
  no_discrepancy_found:'本次比對未發現差異', corrected:'已修正', corrected_with_limits:'已修正，仍有未決項',
  partial_byline:'部分署名不完整', unresolved_mapping:'機構歸屬未決', no_printed_affiliation:'未印出正式機構',
  supported:'來源支持此紀錄', no_publication_located:'尚未找到發表紀錄', corrected_no_publication_located:'已撤銷錯配',
  limited_source_access:'來源存取受限', component_cascade_excluded:'組合系統，不歸屬單一打造者', unresolved_model_identity:'模型／團隊身分未決',
};
const unresolved = value => /unresolved|partial|with_limits|source_access/.test(value);
const sources = rows => '<div class="sources">'+[...new Set(rows.map(x=>x.url).filter(Boolean))].map((url,i)=>'<a href="'+esc(url)+'" target="_blank" rel="noopener">來源 '+(i+1)+'</a>').join(' · ')+'</div>';

export function renderMetadataAudit(audit, corrections, institutions) {
  const names = Object.fromEntries(institutions.entries.map(i=>[i.id,i.name]));
  const s = audit.summary;
  const rows = audit.benchmarks.map(b=>{
    const a=b.affiliation,v=b.publication;
    const state=unresolved(a.outcome)||unresolved(v.outcome)?'unresolved':(a.outcome.includes('corrected')||v.outcome.includes('corrected'))?'corrected':'checked';
    return `<tr data-kind="benchmark" data-state="${state}"><th scope="row">${esc(b.name)}<small>benchmark · ${esc(b.arxiv_id)}</small></th><td><b>${labels[a.outcome]||esc(a.outcome)}</b><p>${esc(a.institutions.map(id=>names[id]||id).join(' · ')||'未歸屬')}</p><details><summary>核對說明</summary><p>${esc(a.reason)}</p>${sources(a.sources)}</details></td><td><b>${esc(v.venue||'arXiv preprint')} · ${esc(v.status)}</b><p>${labels[v.outcome]||esc(v.outcome)}${v.track?' · '+esc(v.track):''}</p><details><summary>核對說明</summary><p>${esc(v.reason)}</p>${sources(v.sources)}</details></td></tr>`;
  });
  rows.push(...audit.models.map(m=>`<tr data-kind="model" data-state="${unresolved(m.outcome)?'unresolved':m.outcome==='corrected'?'corrected':'checked'}"><th scope="row">${esc(m.name)}<small>model · ${esc(m.id)}</small></th><td colspan="2"><b>${labels[m.outcome]||esc(m.outcome)}</b><p>${esc(m.builders.map(id=>names[id]||id).join(' · ')||'未歸屬單一打造者')}</p><details><summary>核對說明</summary><p>${esc(m.reason)}</p>${sources(m.sources)}</details></td></tr>`));
  const changes=corrections.patches.filter(p=>p.section==='benchmarks'||p.section==='models').map(p=>{
    const field=p.dataset==='venues'?'發表':'機構';
    const describe=x=>x==null?'—':x.institutions||x.builders?(x.institutions||x.builders).map(i=>names[i.inst]||i.inst).join(' · '):['status','display','track','publishable'].filter(k=>Object.hasOwn(x,k)).map(k=>k+': '+String(x[k])).join(' · ')||'來源／紀錄欄位';
    return `<tr><th scope="row">${esc(p.id)}<small>${field}</small></th><td>${esc(describe(p.before))}</td><td>${esc(describe(p.after))}<details><summary>更正依據</summary><p>${esc(p.reason)}</p>${sources(p.sources.map(url=>({url})))}</details></td></tr>`;
  }).join('');
  return `<!doctype html><html lang="zh-Hant"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>逐筆資料稽核 · Spoken LLM Benchmark Atlas</title><style>
  :root{color-scheme:light dark;--bg:#f6f5ef;--ink:#203d35;--line:#d5ddd7;--paper:white}*{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--ink);font:16px/1.65 system-ui,sans-serif}main{max-width:1260px;margin:auto;padding:48px 24px}a{color:#176a55;text-underline-offset:3px}h1{font-size:clamp(1.8rem,4vw,2.8rem);line-height:1.25;max-width:900px}h2{margin-top:48px}p{margin:.5em 0}.intro{max-width:900px}.eyebrow{font-size:13px;letter-spacing:.1em}.stats{display:grid;grid-template-columns:repeat(4,1fr);gap:14px;margin:28px 0}.stat{background:var(--paper);border:1px solid var(--line);padding:20px;border-radius:10px}.stat strong{display:block;font-size:2rem}.note{padding:18px 22px;background:#e6eee7;border-left:4px solid #347058;margin:22px 0}.filters{display:flex;gap:12px;flex-wrap:wrap;align-items:center;margin:22px 0}input,select{font:inherit;padding:9px;border:1px solid var(--line);border-radius:6px;background:var(--paper);color:inherit}input{min-width:240px;flex:1}table{width:100%;border-collapse:collapse;background:var(--paper);table-layout:fixed;font-size:14px}th,td{border-bottom:1px solid var(--line);padding:16px;text-align:left;vertical-align:top;overflow-wrap:anywhere}th:first-child{width:27%}thead th{background:#dfe9e1}small{display:block;font-weight:400;opacity:.7;margin-top:6px}details{margin-top:10px}summary{cursor:pointer;color:#287153}.sources{margin-top:10px}.wrap{overflow:auto}tr[hidden]{display:none}.muted{font-size:14px;opacity:.8}@media(max-width:650px){main{padding:28px 14px}.stats{grid-template-columns:repeat(2,1fr)}th,td{padding:10px}table{min-width:700px}input{min-width:100%}}@media(prefers-color-scheme:dark){:root{--bg:#14251f;--ink:#e3ebe5;--line:#3b5147;--paper:#20352c}a,summary{color:#9bd9bb}.note,thead th{background:#2c483a}}
  </style><main><a href="index.html">← Spoken LLM Benchmark Atlas</a><p class="eyebrow">GPT-6 METADATA AUDIT · ${audit.audit_date}</p><h1>每一筆機構與發表紀錄，留下核對依據</h1><div class="intro"><p>本次核對 ${s.benchmarks} 筆 benchmark 的機構與發表紀錄，以及 ${s.models} 筆模型資料。以下提供逐筆結果、來源與更正前後的差異。</p><p>這是 GPT-6 的 AI 稽核，尚未經人類逐筆驗證。「未發現差異」限於所讀來源；未決項目保留說明。</p></div><div class="stats"><div class="stat"><strong>${s.benchmarks}</strong>benchmark 已核對</div><div class="stat"><strong>${s.models}</strong>模型紀錄已核對</div><div class="stat"><strong>${s.benchmark_affiliations_changed+s.model_builders_changed}</strong>機構紀錄修正</div><div class="stat"><strong>${s.venue_records_changed}</strong>發表欄位更新</div></div><div class="note">${s.venue_records_changed} 筆發表欄位更新中，${s.venue_status_changed} 筆改變發表狀態；其餘包含 track、會議分類與來源補強。${s.component_cascades_excluded} 筆由第三方元件組合的系統，繼續排除單一打造機構的歸屬。</div><p><a href="#records">逐筆結果</a> · <a href="#changes">更正前後</a> · <a href="${'https://github.com/speechlab0210/spoken-llm-benchmarks/blob/main/data/metadata-audit-2026-09-06.json'}">完整 JSON 紀錄</a></p><h2>核對方法與限制</h2><ul>${audit.method.map(x=>'<li>'+esc(x)+'</li>').join('')}</ul><p>「尚未找到發表紀錄」是截至本次搜尋日期的結果。正式 proceedings、官方接受／發表議程，以及作者自行填寫的接受聲明，分別保留不同證據層級。校區合併仍保留原地點；未逐一重新測繪所有預設座標。</p><p>仍待釐清：OpenNLP Lab 的母機構、Hupan Lab 在母公司統計規則下是否應獨立計數；另有部分論文署名與精確模型身分不足。可使用下方「未決／部分資料」篩選器查看。</p><h2 id="records">逐筆結果</h2><div class="filters"><label for="query">搜尋</label><input id="query" type="search" placeholder="名稱、arXiv、機構或會議"><label for="kind">類型</label><select id="kind"><option value="all">全部</option><option value="benchmark">Benchmark</option><option value="model">Model</option></select><label for="state">結果</label><select id="state"><option value="all">全部</option><option value="corrected">已更正</option><option value="unresolved">未決／部分資料</option></select><span id="count" role="status"></span></div><div class="wrap"><table id="records-table"><thead><tr><th>項目</th><th>打造機構／論文機構</th><th>發表紀錄</th></tr></thead><tbody>${rows.join('')}</tbody></table></div><h2 id="changes">更正前後</h2><p class="muted">來源補強列在同一張表，避免把補強來源誤報成會議填錯。完整的欄位差異與來源在 <a href="https://github.com/speechlab0210/spoken-llm-benchmarks/blob/main/data/metadata-corrections.json">更正紀錄</a>。</p><div class="wrap"><table><thead><tr><th>紀錄</th><th>原值</th><th>修正值與依據</th></tr></thead><tbody>${changes}</tbody></table></div></main><script>
  const rows=[...document.querySelectorAll('#records-table tbody tr')],query=document.querySelector('#query'),kind=document.querySelector('#kind'),state=document.querySelector('#state');
  function filter(){let count=0;for(const row of rows){const match=(!query.value||row.textContent.toLowerCase().includes(query.value.toLowerCase()))&&(kind.value==='all'||row.dataset.kind===kind.value)&&(state.value==='all'||row.dataset.state===state.value);row.hidden=!match;if(match)count++;}document.querySelector('#count').textContent=count+' / '+rows.length+' 筆';}
  for(const control of [query,kind,state])control.addEventListener('input',filter);filter();
  </script></html>`;
}
